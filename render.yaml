services:
  - type: web
    name: papervault-api
    runtime: node
    region: singapore
    rootDir: server
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: SYNC_DB
        value: false
